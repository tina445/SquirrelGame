{
  "name": "@squirrel-heist/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build -w @squirrel-heist/shared && tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run tests",
    "bot:evaluate": "node dist/bot/evaluateBots.js"
  },
  "dependencies": {
    "@squirrel-heist/bot-core": "*",
    "@squirrel-heist/shared": "*",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/ws": "^8.18.1"
  }
}
