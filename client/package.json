{
  "name": "@squirrel-heist/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build -w @squirrel-heist/shared && vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@squirrel-heist/shared": "*",
    "@tweenjs/tween.js": "^25.0.0",
    "three": "^0.179.1"
  },
  "devDependencies": {
    "@types/three": "^0.179.0",
    "vite": "^7.1.3"
  }
}
